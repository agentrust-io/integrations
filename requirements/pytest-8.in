# Compile with:
#   uv pip compile requirements/pytest-8.in --generate-hashes --universal \
#     --python-version 3.9 -o requirements/pytest-8.txt
# pytest 8.4.2, pinned by the agentrust-codex no-signing job. Kept distinct
# from pytest-9 rather than unified: the divergence is undocumented and
# changing a test runner version is not this change's business.
pytest==8.4.2
