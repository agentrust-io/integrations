name: Check documentation links

on:
  pull_request:
    paths:
      - "**/*.md"
      - "**/integration.yaml"
  schedule:
    - cron: "17 8 * * 1"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  links:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: lycheeverse/lychee-action@2ac5c5c9b4b4f6d4fbd2ea739fce55df3fce2635 # v2.6.1
        with:
          args: >-
            --no-progress
            --exclude-path .nox
            --exclude-path .pytest_cache
            --max-retries 3
            '**/*.md'
            '**/integration.yaml'
          fail: true
